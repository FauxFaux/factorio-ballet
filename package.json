{
  "name": "faucalc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "bundle:analyze": "tsc -b && vite build --mode analyze",
    "deploy:snapshot": "npm run build && rsync -av --delete dist/ sek:/srv/ballet.goeswhere.com/snapshot-$(git rev-parse --short HEAD)/",
    "format": "oxfmt",
    "lint": "eslint . && tsc -b",
    "preview": "vite preview",
    "split-icons": "node scripts/split-icons.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3",
    "@primer/octicons-react": "^19",
    "fflate": "^0.8",
    "preact": "^10",
    "proc-web": "file:../../clone/proc-rs/proc-web/pkg",
    "serialize-error": "^13",
    "zod": "^4"
  },
  "devDependencies": {
    "@eslint/js": "^10",
    "@preact/preset-vite": "^2",
    "@types/node": "^24",
    "eslint": "^10",
    "factorio-raw-types": "^0.4",
    "oxfmt": "^0.64",
    "sharp": "^0.35",
    "typescript": "~6",
    "typescript-eslint": "^8",
    "vite": "^8",
    "vite-bundle-analyzer": "^1",
    "vitest": "^4"
  }
}
