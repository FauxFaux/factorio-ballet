{
  "name": "faucalc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "deploy:snapshot": "npm run build && rsync -av --delete dist/ sek:/srv/ballet.goeswhere.com/snapshot-$(git rev-parse --short HEAD)/",
    "format": "oxfmt",
    "lint": "eslint . && tsc -b",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@primer/octicons-react": "^19.33.0",
    "fflate": "^0.8",
    "preact": "^10",
    "serialize-error": "^13",
    "zod": "^4"
  },
  "devDependencies": {
    "@eslint/js": "^10",
    "@preact/preset-vite": "^2",
    "@types/node": "^24",
    "eslint": "^10",
    "factorio-raw-types": "^0.4",
    "oxfmt": "^0.64",
    "typescript": "~6",
    "typescript-eslint": "^8",
    "vite": "^8",
    "vitest": "^4"
  }
}
